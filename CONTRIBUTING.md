# Contributing

1. Install dependencies with `npm install`.
2. Make source changes under `src/` and add or update TypeScript tests.
3. Run `npm run validate`.
4. Document public behavior and configuration changes.

Keep changes focused, preserve typed boundaries, and include failure-path coverage for new runtime behavior.
