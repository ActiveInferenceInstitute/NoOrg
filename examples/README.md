# Examples

`basic-workflow.ts` constructs the runtime with the local provider, registers an executable analysis agent, submits a task, waits for the real task transition, and shuts down all resources.
